import React from "react";
import { Form } from "react-bootstrap";

function SearchBar() {
  return (
    <Form.Control type="text" placeholder="Search for an instrument or role!" />
  );
}

export default SearchBar;
